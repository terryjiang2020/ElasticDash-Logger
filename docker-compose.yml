# Make sure to update the credential placeholders with your own secrets.
# We mark them with # CHANGEME in the file below.
# In addition, we recommend to restrict inbound traffic on the host to langfuse-web (port 3000) and minio (port 9090) only.
# All other components are bound to localhost (127.0.0.1) to only accept connections from the local machine.
# External connections from other machines will not be able to reach these services directly.
services:
  langfuse-worker:
    image: docker.io/langfuse/langfuse-worker:3
    restart: always
    depends_on: &langfuse-depends-on
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      redis:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    ports:
      - 127.0.0.1:3030:3030
    environment: &langfuse-worker-env
      NEXTAUTH_URL: ${NEXTAUTH_URL}
      DATABASE_URL: ${DATABASE_URL} # CHANGEME
      SALT: ${SALT} # CHANGEME
      ENCRYPTION_KEY: ${ENCRYPTION_KEY} # CHANGEME: generate via `openssl rand -hex 32`
      TELEMETRY_ENABLED: ${TELEMETRY_ENABLED:-true}
      ELASTICDASH_ENABLE_EXPERIMENTAL_FEATURES: ${ELASTICDASH_ENABLE_EXPERIMENTAL_FEATURES:-true}
      CLICKHOUSE_MIGRATION_URL: ${CLICKHOUSE_MIGRATION_URL:-clickhouse://clickhouse:9000}
      CLICKHOUSE_URL: ${CLICKHOUSE_URL:-http://clickhouse:8123}
      CLICKHOUSE_USER: ${CLICKHOUSE_USER:-clickhouse}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-clickhouse} # CHANGEME
      CLICKHOUSE_CLUSTER_ENABLED: ${CLICKHOUSE_CLUSTER_ENABLED:-false}
      ELASTICDASH_USE_AZURE_BLOB: ${ELASTICDASH_USE_AZURE_BLOB:-false}
      ELASTICDASH_S3_EVENT_UPLOAD_BUCKET: ${ELASTICDASH_S3_EVENT_UPLOAD_BUCKET:-langfuse}
      ELASTICDASH_S3_EVENT_UPLOAD_REGION: ${ELASTICDASH_S3_EVENT_UPLOAD_REGION:-auto}
      ELASTICDASH_S3_EVENT_UPLOAD_ACCESS_KEY_ID: ${ELASTICDASH_S3_EVENT_UPLOAD_ACCESS_KEY_ID:-minio}
      ELASTICDASH_S3_EVENT_UPLOAD_SECRET_ACCESS_KEY: ${ELASTICDASH_S3_EVENT_UPLOAD_SECRET_ACCESS_KEY:-miniosecret} # CHANGEME
      ELASTICDASH_S3_EVENT_UPLOAD_ENDPOINT: ${ELASTICDASH_S3_EVENT_UPLOAD_ENDPOINT:-http://minio:9000}
      ELASTICDASH_S3_EVENT_UPLOAD_FORCE_PATH_STYLE: ${ELASTICDASH_S3_EVENT_UPLOAD_FORCE_PATH_STYLE:-true}
      ELASTICDASH_S3_EVENT_UPLOAD_PREFIX: ${ELASTICDASH_S3_EVENT_UPLOAD_PREFIX:-events/}
      ELASTICDASH_S3_MEDIA_UPLOAD_BUCKET: ${ELASTICDASH_S3_MEDIA_UPLOAD_BUCKET:-langfuse}
      ELASTICDASH_S3_MEDIA_UPLOAD_REGION: ${ELASTICDASH_S3_MEDIA_UPLOAD_REGION:-auto}
      ELASTICDASH_S3_MEDIA_UPLOAD_ACCESS_KEY_ID: ${ELASTICDASH_S3_MEDIA_UPLOAD_ACCESS_KEY_ID:-minio}
      ELASTICDASH_S3_MEDIA_UPLOAD_SECRET_ACCESS_KEY: ${ELASTICDASH_S3_MEDIA_UPLOAD_SECRET_ACCESS_KEY:-miniosecret} # CHANGEME
      ELASTICDASH_S3_MEDIA_UPLOAD_ENDPOINT: ${ELASTICDASH_S3_MEDIA_UPLOAD_ENDPOINT:-http://minio:9000}
      ELASTICDASH_S3_MEDIA_UPLOAD_FORCE_PATH_STYLE: ${ELASTICDASH_S3_MEDIA_UPLOAD_FORCE_PATH_STYLE:-true}
      ELASTICDASH_S3_MEDIA_UPLOAD_PREFIX: ${ELASTICDASH_S3_MEDIA_UPLOAD_PREFIX:-media/}
      ELASTICDASH_S3_BATCH_EXPORT_ENABLED: ${ELASTICDASH_S3_BATCH_EXPORT_ENABLED:-false}
      ELASTICDASH_S3_BATCH_EXPORT_BUCKET: ${ELASTICDASH_S3_BATCH_EXPORT_BUCKET:-langfuse}
      ELASTICDASH_S3_BATCH_EXPORT_PREFIX: ${ELASTICDASH_S3_BATCH_EXPORT_PREFIX:-exports/}
      ELASTICDASH_S3_BATCH_EXPORT_REGION: ${ELASTICDASH_S3_BATCH_EXPORT_REGION:-auto}
      ELASTICDASH_S3_BATCH_EXPORT_ENDPOINT: ${ELASTICDASH_S3_BATCH_EXPORT_ENDPOINT:-http://minio:9000}
      ELASTICDASH_S3_BATCH_EXPORT_EXTERNAL_ENDPOINT: ${ELASTICDASH_S3_BATCH_EXPORT_EXTERNAL_ENDPOINT:-http://localhost:9090}
      ELASTICDASH_S3_BATCH_EXPORT_ACCESS_KEY_ID: ${ELASTICDASH_S3_BATCH_EXPORT_ACCESS_KEY_ID:-minio}
      ELASTICDASH_S3_BATCH_EXPORT_SECRET_ACCESS_KEY: ${ELASTICDASH_S3_BATCH_EXPORT_SECRET_ACCESS_KEY:-miniosecret} # CHANGEME
      ELASTICDASH_S3_BATCH_EXPORT_FORCE_PATH_STYLE: ${ELASTICDASH_S3_BATCH_EXPORT_FORCE_PATH_STYLE:-true}
      ELASTICDASH_INGESTION_QUEUE_DELAY_MS: ${ELASTICDASH_INGESTION_QUEUE_DELAY_MS:-}
      ELASTICDASH_INGESTION_CLICKHOUSE_WRITE_INTERVAL_MS: ${ELASTICDASH_INGESTION_CLICKHOUSE_WRITE_INTERVAL_MS:-}
      REDIS_HOST: ${REDIS_HOST:-redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_AUTH: ${REDIS_AUTH:-myredissecret} # CHANGEME
      REDIS_TLS_ENABLED: ${REDIS_TLS_ENABLED:-false}
      REDIS_TLS_CA: ${REDIS_TLS_CA:-/certs/ca.crt}
      REDIS_TLS_CERT: ${REDIS_TLS_CERT:-/certs/redis.crt}
      REDIS_TLS_KEY: ${REDIS_TLS_KEY:-/certs/redis.key}
      EMAIL_FROM_ADDRESS: ${EMAIL_FROM_ADDRESS:-}
      SMTP_CONNECTION_URL: ${SMTP_CONNECTION_URL:-}

  langfuse-web:
    image: docker.io/langfuse/langfuse:3
    restart: always
    depends_on: *langfuse-depends-on
    ports:
      - 3002:3000
    environment:
      <<: *langfuse-worker-env
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET} # CHANGEME
      ELASTICDASH_INIT_ORG_ID: ${ELASTICDASH_INIT_ORG_ID:-}
      ELASTICDASH_INIT_ORG_NAME: ${ELASTICDASH_INIT_ORG_NAME:-}
      ELASTICDASH_INIT_PROJECT_ID: ${ELASTICDASH_INIT_PROJECT_ID:-}
      ELASTICDASH_INIT_PROJECT_NAME: ${ELASTICDASH_INIT_PROJECT_NAME:-}
      ELASTICDASH_INIT_PROJECT_PUBLIC_KEY: ${ELASTICDASH_INIT_PROJECT_PUBLIC_KEY:-}
      ELASTICDASH_INIT_PROJECT_SECRET_KEY: ${ELASTICDASH_INIT_PROJECT_SECRET_KEY:-}
      ELASTICDASH_INIT_USER_EMAIL: ${ELASTICDASH_INIT_USER_EMAIL:-}
      ELASTICDASH_INIT_USER_NAME: ${ELASTICDASH_INIT_USER_NAME:-}
      ELASTICDASH_INIT_USER_PASSWORD: ${ELASTICDASH_INIT_USER_PASSWORD:-}

  clickhouse:
    image: docker.io/clickhouse/clickhouse-server
    restart: always
    user: "101:101"
    environment:
      CLICKHOUSE_DB: default
      CLICKHOUSE_USER: ${CLICKHOUSE_USER:-clickhouse}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD:-clickhouse} # CHANGEME
    volumes:
      - langfuse_clickhouse_data:/var/lib/clickhouse
      - langfuse_clickhouse_logs:/var/log/clickhouse-server
    ports:
      - 127.0.0.1:8123:8123
      - 127.0.0.1:9000:9000
    healthcheck:
      test: wget --no-verbose --tries=1 --spider http://localhost:8123/ping || exit 1
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 1s

  minio:
    image: cgr.dev/chainguard/minio
    restart: always
    entrypoint: sh
    # create the 'langfuse' bucket before starting the service
    command: -c 'mkdir -p /data/langfuse && minio server --address ":9000" --console-address ":9001" /data'
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minio}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-miniosecret} # CHANGEME
    ports:
      - 9090:9000
      - 127.0.0.1:9091:9001
    volumes:
      - langfuse_minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 1s
      timeout: 5s
      retries: 5
      start_period: 1s

  redis:
    image: docker.io/redis:7
    restart: always
    # CHANGEME: row below to secure redis password
    command: >
      --requirepass ${REDIS_AUTH:-myredissecret}
      --maxmemory-policy noeviction
    ports:
      - 127.0.0.1:6379:6379
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 10s
      retries: 10

  postgres:
    image: docker.io/postgres:${POSTGRES_VERSION:-17}
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -p 5433"]
      interval: 3s
      timeout: 3s
      retries: 10
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres} # CHANGEME
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
      TZ: UTC
      PGTZ: UTC
    ports:
      - 127.0.0.1:5433:5433
    command: -p 5433
    volumes:
      - langfuse_postgres_data:/var/lib/postgresql/data

volumes:
  langfuse_postgres_data:
    driver: local
  langfuse_clickhouse_data:
    driver: local
  langfuse_clickhouse_logs:
    driver: local
  langfuse_minio_data:
    driver: local
